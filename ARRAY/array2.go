package main

import "fmt"

func main() {
    var sueldos [5]int
    for i := 0; i < 5; i++ {
        fmt.Print("Ingrese valor del sueldo:")
        fmt.Scan(&sueldos[f])
    }
    fmt.Println("Listado de sueldos.")
    fmt.Println(sueldos)//imprimir cadena de sueldos
}